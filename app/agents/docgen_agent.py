import os
from anthropic import Anthropic

MODEL = "claude-haiku-4-5-20251001"

_PROMPTS = {
    "sick_note": """\
אתה מזכיר רפואי מקצועי. כתוב אישור מחלה רשמי בעברית.
פורמט חובה:
• כותרת: אישור מחלה
• שם המטופל ומספר תעודת זהות
• תאריכי היעדרות מהעבודה/הלימודים
• אבחנה כללית בלבד (ללא פרטים מסווגים)
• שורת חתימה לרופא ותאריך
החזר רק את טקסט המסמך בעברית, ללא הסברים נוספים.""",

    "referral": """\
אתה מזכיר רפואי מקצועי. כתוב מכתב הפניה רפואי בעברית.
פורמט חובה:
• כותרת: מכתב הפניה
• לכבוד: [מומחה/מחלקה]
• פרטי המטופל (שם, גיל, ת.ז)
• סיבת ההפניה
• ממצאים קליניים רלוונטיים
• בקשה לבדיקה/ייעוץ/טיפול
• פרטי הרופא המפנה וחתימה
החזר רק את טקסט המכתב בעברית, ללא הסברים נוספים.""",

    "prescription": """\
אתה מזכיר רפואי מקצועי. כתוב מרשם רפואי בעברית.
פורמט חובה:
• כותרת: מרשם
• פרטי המטופל
• שם התרופה, מינון, תדירות, משך טיפול
• הנחיות לשימוש
• תאריך ופרטי הרופא
החזר רק את טקסט המרשם בעברית, ללא הסברים נוספים.""",

    "fitness": """\
אתה מזכיר רפואי מקצועי. כתוב תעודת כשירות רפואית בעברית.
פורמט חובה:
• כותרת: תעודת כשירות רפואית
• פרטי המטופל
• מטרת הכשירות (ספורט/עבודה/נהיגה וכו׳)
• מסקנה: כשיר/לא כשיר
• תוקף התעודה
• פרטי הרופא וחתימה
החזר רק את טקסט התעודה בעברית, ללא הסברים נוספים.""",
}


def generate_document(doc_type: str, patient_name: str, details: dict) -> str:
    client = Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])
    system = _PROMPTS.get(doc_type, _PROMPTS["referral"])

    parts = [f"שם מלא: {patient_name}"]
    for k, v in details.items():
        if v:
            parts.append(f"{k}: {v}")

    response = client.messages.create(
        model=MODEL,
        max_tokens=1024,
        system=system,
        messages=[{"role": "user", "content": "\n".join(parts)}],
    )
    return response.content[0].text.strip()
